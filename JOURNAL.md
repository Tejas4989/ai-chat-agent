Day 2 ✅ Fluent API + PromptTemplate + /draft. System = persona. User = request.
Day 3 ✅ .stream().content() = Flux<String>. produces=TEXT_EVENT_STREAM_VALUE wires it to SSE. fetch() ReadableStream reads it in browser.
